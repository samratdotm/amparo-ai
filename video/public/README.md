# Assets

Drop your screen recording here as **`demo.mp4`**, then set `HAS_RECORDING = true`
in `src/scenes/DemoScene.tsx`. The animated browser frame will wrap around it.

Until then, the demo scene renders an animated re-creation of the live comparison
panel, so the video looks complete out of the box.
